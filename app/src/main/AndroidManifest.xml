<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dfgstudio.listadetarefas">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_final_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_final_24"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListaDeTarefas">
        <activity
            android:name=".activity.AdicionarTarefaMainActivity"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ListaDeTarefas.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>